<!-- DiagnosisResponse.vue -->
<template>
    <!-- Message list -->
    <div class="flex-1 overflow-y-auto space-y-4 px-2 pt-2 pb-64 scrollbar-hide h-full">
        <!-- <div v-for="(msg, index) in messages" :key="index">
            <div v-if="msg.sender === 'user'" class="chat chat-end">
                <div class="chat-bubble chat-bubble-neutral">
                    {{ store.message }}
                </div>
            </div>
            <div v-else class="p-4 text-sm">
                {{ msg.text }}
            </div>
        </div> -->
    </div>
</template>


<script setup>
</script>